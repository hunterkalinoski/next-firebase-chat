"use client";

import Counter from "@components/Counter";

export default function Home() {
  return (
    <main className="bg-slate-700">
      <div>Hello new next project with firebase + tailwind + typescript</div>
      <Counter />
    </main>
  );
}
